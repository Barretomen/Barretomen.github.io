name: Build temporary Wallet photo

on:
  workflow_dispatch:
  push:
    paths:
      - '.github/workflows/build-wallet-photo-temp.yml'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Rebuild JPEG
        run: |
          cat assets/wallet/.photo_part_00.b64 assets/wallet/.photo_part_01.b64 assets/wallet/.photo_part_02.b64 | base64 -d > assets/wallet/joao_barreto_wallet_photo.jpg
          test "$(stat -c%s assets/wallet/joao_barreto_wallet_photo.jpg)" -eq 22395
          file assets/wallet/joao_barreto_wallet_photo.jpg
      - name: Commit photo and clean temporary files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add assets/wallet/joao_barreto_wallet_photo.jpg
          git rm assets/wallet/.photo_part_00.b64 assets/wallet/.photo_part_01.b64 assets/wallet/.photo_part_02.b64
          git rm .github/workflows/build-wallet-photo-temp.yml
          git commit -m "Add João Barreto Wallet profile photo"
          git push
